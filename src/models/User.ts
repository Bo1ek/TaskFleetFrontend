export type UserProfileToken = {
    email: string;
    firstName: string;
    lastName: string;
    role: string;
    token: string;
}

export type UserProfile = {
    email: string;
    firstName: string;
    lastName: string;
    role: string;
}